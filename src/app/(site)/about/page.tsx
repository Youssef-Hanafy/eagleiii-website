import Link from 'next/link'; import { getSiteSettings } from '@/lib/data';
export default async function AboutPage(){ const s=await getSiteSettings(); return <section className="page-section"><div className="shell narrow"><div className="eyebrow dark">ABOUT THE BUSINESS</div><h1>{s.about_title}</h1><p className="lead">{s.about_text}</p><div className="content-card"><h2>Our approach</h2><p>The business is focused on connecting quality Egyptian products with buyers that need commercial quantities. Rather than operating like a retail store, we work through direct inquiries so product requirements, quantities, packaging, documentation, and logistics can be discussed for each order.</p></div><Link className="button button-dark" href="/contact">Start an Inquiry</Link></div></section>; }
