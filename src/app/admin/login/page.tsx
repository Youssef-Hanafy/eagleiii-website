import { login } from './actions';
export default async function AdminLoginPage({searchParams}:{searchParams:Promise<{error?:string}>}){ const p=await searchParams; return <section className="admin-login-page"><div className="admin-login-card"><div className="brand admin-brand"><span className="brand-mark">E</span><span><strong>EGYPT HERBS</strong><small>Admin Portal</small></span></div><h1>Welcome back.</h1><p>Sign in to manage products, photos, contact information, website content, and buyer inquiries.</p>{p.error&&<div className="error-box">{p.error==='unauthorized'?'This account is not authorized as an admin.':'Incorrect email or password.'}</div>}<form action={login} className="admin-login-form"><label>Email<input name="email" type="email" required placeholder="admin@company.com"/></label><label>Password<input name="password" type="password" required placeholder="••••••••"/></label><button className="button button-dark" type="submit">Sign In</button></form></div></section>; }
