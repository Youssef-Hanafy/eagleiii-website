import Link from 'next/link'; import { notFound } from 'next/navigation'; import { createClient } from '@/lib/supabase/server'; import { ProductAdminForm } from '@/components/ProductAdminForm'; import { updateProduct,deleteGalleryImage } from '../actions'; import type {Product} from '@/lib/types';
export default async function EditProduct({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{saved?:string;error?:string}>}){const [{id},q,s]=await Promise.all([params,searchParams,createClient()]);const {data}=await s.from('products').select('*, product_images(*)').eq('id',id).single(); if(!data)notFound(); const p=data as Product; return <><div className="admin-heading"><div><small>EDIT PRODUCT</small><h1>{p.name}</h1></div><Link className="button button-soft" href="/admin/products">Back to Products</Link></div>{q.saved&&<div className="success-box">Changes saved and the website has been refreshed.</div>}{q.error&&<div className="error-box">There was a problem saving your changes.</div>}<ProductAdminForm product={p} action={updateProduct}/>{p.product_images?.length?<div className="admin-form-section gallery-admin"><div className="form-section-heading"><div><small>GALLERY</small><h2>Existing gallery photos</h2></div></div><div className="admin-gallery-grid">{p.product_images.map(img=><div key={img.id}><img src={img.image_url} alt=""/><form action={deleteGalleryImage}><input type="hidden" name="image_id" value={img.id}/><input type="hidden" name="product_id" value={p.id}/><button type="submit">Remove</button></form></div>)}</div></div>:null}</>}
