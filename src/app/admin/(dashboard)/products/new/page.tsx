import Link from 'next/link'; import { ProductAdminForm } from '@/components/ProductAdminForm'; import { createProduct } from '../actions';
export default async function NewProduct({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams; return <><div className="admin-heading"><div><small>PRODUCTS</small><h1>Add product</h1></div><Link className="button button-soft" href="/admin/products">Back</Link></div>{p.error&&<div className="error-box">Could not create the product. Check the fields and upload files, then try again.</div>}<ProductAdminForm action={createProduct}/></>}
